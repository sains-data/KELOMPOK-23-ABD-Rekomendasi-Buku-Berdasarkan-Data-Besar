LOAD_EX=n
EXECUTOR=Celery
# AIRFLOW__CORE__FERNET_KEY= # akan digenerate jika kosong (biarkan ini dikomentari atau set nilainya)

# --- Konfigurasi Koneksi PostgreSQL untuk puckel/docker-airflow ---
POSTGRES_HOST=postgres-airflow  # Nama service database PostgreSQL Anda
POSTGRES_PORT=5432              # Port internal PostgreSQL
POSTGRES_USER=airflow           # Sesuai environment di docker-compose untuk postgres-airflow
POSTGRES_PASSWORD=airflow       # Sesuai environment di docker-compose untuk postgres-airflow
POSTGRES_DB=airflow             # Sesuai environment di docker-compose untuk postgres-airflow

# --- Konfigurasi Koneksi Redis untuk Celery ---
REDIS_HOST=redis-airflow      # Nama service Redis Anda
REDIS_PORT=6379               # Port internal Redis
# REDIS_PASSWORD=              # Jika Redis Anda menggunakan password (tidak di setup kita)

# Baris-baris di bawah ini bisa tetap dikomentari untuk image puckel/docker-airflow:1.10.9
# Jika ada masalah, baru kita coba aktifkan dan sesuaikan:
# AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres-airflow:5432/airflow
# AIRFLOW__CELERY__BROKER_URL=redis://redis-airflow:6379/0
# AIRFLOW__CELERY__RESULT_BACKEND=db+postgresql://airflow:airflow@postgres-airflow:5432/airflow
# AIRFLOW__CORE__LOAD_EXAMPLES=False